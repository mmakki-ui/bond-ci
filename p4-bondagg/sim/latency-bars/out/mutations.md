# Mutation matrix -- proving each OBJ-D latency bar can go RED (U14)

GENERATED by `mutation_matrix.py` from `out/mut_*.txt`. Do not hand-edit.

Every defect is a REAL design error from this project's own record, not a
synthetic mutation. Run at SEEDS=2 (defects are gross; the point is the
VERDICT flipping, not a margin), GEO=canonical.

| defect | what it is |
|---|---|
| `hold-gran` | the ratchet never learns -- a bare granularity hold |
| `rank-hungriest` | `speed` silently degraded into `max` -- hungriest-first |
| `rank-mid-meter` | the cap meter fed into the mid draw KEY (g2m). REFUTED r1 sec 5: p95 456 vs 371 at mid 0.85 |
| `rank-static` | draw order = static latency rank (V1). REFUTED r1 sec 3.2, and it reproduces CPF's documented pinning failure: -9.07% gp at S3@90k |
| `ratchet-x3` | granularity inflated 3x, so the floor becomes a pad again. This is r1 sec 8's OWN pre-registered pass-by-artifact route ("granularity inflation -- gran becomes the new pad"), and the guard it names is exactly HOLD-4 |
| `warmup-max` | the ring arms at HoldMax(343) instead of granularity -- the exact bug r1 sec 4.4 deletes ("warm-up = HoldMax is backwards") |

| gated bar | class | reddened by |
|---|---|---|
| `HOLD-1a` | NO RED DEMO | **nothing** |
| `HOLD-1b` | NO RED DEMO | **nothing** |
| `HOLD-1d` | RED-DEMONSTRATED | `hold-gran` |
| `HOLD-2b` | RED-DEMONSTRATED | `hold-gran` |
| `HOLD-3a` | NO RED DEMO | **nothing** |
| `HOLD-3b` | NO RED DEMO | **nothing** |
| `HOLD-4a` | RED-DEMONSTRATED | `ratchet-x3` |
| `HOLD-4b` | RED-DEMONSTRATED | `ratchet-x3` |
| `HOLD-4c` | RED-DEMONSTRATED | `ratchet-x3` |
| `SPD-1a` | RED-DEMONSTRATED | `rank-hungriest` |
| `SPD-1b` | RED-DEMONSTRATED | `rank-hungriest` |
| `SPD-1c` | NO RED DEMO | **nothing** |
| `SPD-1d` | NO RED DEMO | **nothing** |
| `SPD-2a` | RED-DEMONSTRATED | `rank-static` |
| `SPD-2b` | MUST-FAIL | fails on the clean tree |
| `SPD-2c` | RED-DEMONSTRATED | `rank-hungriest` |
| `SPD-3a` | RED-DEMONSTRATED | `hold-gran` |
| `SPD-5a` | MUST-FAIL | fails on the clean tree |
| `SPD-6a` | RED-DEMONSTRATED | `rank-hungriest` |
| `SPD-6b` | RED-DEMONSTRATED | `rank-hungriest` |
| `SPD-6c` | RED-DEMONSTRATED | `rank-hungriest` |

## Bars with NO demonstrated failure mode

Reported here and NOT gated. A bar that cannot fail is not a gate, and
merging it green would be exactly the theatre this unit exists to avoid.

- **`HOLD-1a`** -- r1 sec 4.1 measured it and this reproduces it: AT THE EDGE the hold length moves no percentile at all -- in-order frames release on arrival, so p50 is 12 ms under the ratchet, under a bare granularity hold, under a x3 pad and under fixed-343 alike. The hold is a LOSS knob at the edge. A latency bar there cannot fail.
- **`HOLD-1b`** -- same as HOLD-1a: p95 is 242 ms under every hold policy injected.
- **`HOLD-3a`** -- the warm-window p95 (12 ms edge) is compared against the OVERALL p95 (242 ms), which is set by the loaded steady state. Arming the ring at HoldMax instead of granularity -- the exact bug r1 sec 4.4 deletes -- moves the warm window to 12 ms at edge and 76 ms at mid, both still far under the overall p95. The bar as r1 states it is absorbed by its own reference. It needs a different reference (the same window under the other warm-up policy) to have teeth.
- **`HOLD-3b`** -- same as HOLD-3a: warm 76 ms vs overall 422 ms under the injected warm-up-at-HoldMax defect.
- **`SPD-1c`** -- the +2*gran tolerance is 20 ms at the model's real granularity (DT=0.010), and the whole spread between "use every source" and "use the fastest one" at an offer that fits one source is 3 ms (p95 14 vs 11 under rank-hungriest). The bar is swamped by its own tolerance. It had teeth in the r1 table only because that table used TICK=1.0 ms, which contradicts the model.
- **`SPD-1d`** -- at an offer the fastest source carries alone, every draw order delivers every frame: gp is 59926 for speed, for max and for the N=1 control alike. There is nothing for a draw-order defect to cost. The bar is true by the offer, not by the scheduler.

```python
MUTATIONS = {
    'hold-gran': ('HOLD-1d', 'HOLD-2b', 'SPD-3a'),
    'rank-hungriest': ('SPD-1a', 'SPD-1b', 'SPD-2c', 'SPD-6a', 'SPD-6b', 'SPD-6c'),
    'rank-static': ('SPD-2a'),
    'ratchet-x3': ('HOLD-4a', 'HOLD-4b', 'HOLD-4c'),
}
NO_RED_DEMO = ('HOLD-1a', 'HOLD-1b', 'HOLD-3a', 'HOLD-3b', 'SPD-1c', 'SPD-1d')
```
